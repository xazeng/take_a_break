# 起来歇会儿：设计说明

## 范围和兼容性

原生 Windows 托盘休息工具，C++17 + Win32；Windows 10 1607+ / Windows 11 x64。首版没有历史配置迁移要求，不依赖 Qt、Electron 或 .NET。MSVC 静态运行库使发布无需附带 VC 运行库 DLL；仍依赖 Windows 系统 DLL。

采用登录后运行的用户进程，不安装服务。Windows 服务位于 Session 0，若使用服务还需用户会话进程和受控 IPC；本项目目前没有全机策略或后台守护需求，不增加该复杂度。

## 模块

图标采用 A 方案“伸展人物＋计时环”，素材位于 `assets/`。多尺寸 ICO 通过资源脚本嵌入 EXE，托盘与窗口共享按系统大小加载的图标句柄；设置窗口显式设置大小图标。运行时不读取外部图标文件。

| 文件 | 职责 |
| --- | --- |
| `src/main.cpp` | 隐藏消息窗口、托盘、会话和电源事件、原生设置对话框、GDI 休息窗口 |
| `src/model.h` | 配置值、提醒间隔计算、倒计时和会话可提醒条件 |
| `src/config.cpp` | INI 校验与原子替换、当前用户自启动项、调试日志 |
| `src/localization.h` | 中英文文案、Windows 用户界面语言选择及英文回退 |
| `src/app.rc` | 设置对话框、程序清单和版本信息 |
| `tests/tests.cpp` | 调度边界、INI 校验及旧配置兼容测试 |

隐藏顶层工具窗口接收 Windows 广播，无任务栏按钮。每个显示器各创建一个无边框全屏工具窗口，使用 `HWND_TOPMOST` 置顶；仅在休息期间存在。设置窗口按需创建。每个 Windows 会话通过 `Local` 命名互斥量只运行一个实例；重复启动发送注册的设置唤起消息后退出，不读取或改写生产配置。Explorer 重启后重建托盘图标。

主实例使用命名就绪事件处理紧邻启动时尚未建窗的竞争。重复实例先寻找对应隐藏窗口，必要时最多等待 5 秒，再用带 2 秒超时的消息请求显示设置，并允许主实例获取前台焦点。消息没有数据载荷，不传递路径或写入指令；仅对这个消息放行跨完整性级别的窗口通知。主实例将请求排入消息队列，若正在休息或会话不可用则保留请求，在休息结束或会话恢复后显示设置；已有设置窗口则恢复显示并置前。通知失败提供本地化提示，旧版本不支持消息时不会重复启动第二个驻留实例。

## 调度与会话

- 默认普通权限、仅提醒模式、间隔 60 分钟、休息 10 秒。
- 启动并确认会话已连接且未锁定后，从头开始计时；登录自启动可能被 Windows 延迟，计时从实际程序启动起算。
- 间隔模式在休息结束、保存设置、恢复提醒、解锁或会话重连后开启完整新周期。
- 可选整点模式按本地时间计算下一个整点，收到系统时间变更时重新计算；整点模式优先于间隔设置。
- 锁屏、注销、会话断开和系统挂起会取消当前休息，丢弃旧周期。
- 解锁、重新连接或恢复运行时重新检查会话，不补发错过的提醒。
- 手动暂停跨解锁保持；仅手动恢复才重新启动提醒。暂停状态不跨程序重启保存。
- 手动“立即休息”不取消暂停状态。设置窗口打开时不自动打断设置，延后到下一周期。
- 每次弹窗前再次检查 WTS 会话状态和输入桌面；安全桌面或查询失败时不显示提醒。

空闲时仅设置一个提醒定时器，触发后销毁并按下一周期重设，不做定期状态轮询。保存单调时钟截止时间，以忽略取消或重置前已排队的旧定时器消息。休息期间以 200 毫秒 UI 定时器刷新文本，剩余时间由 `GetTickCount64` 的截止时间计算，不依赖消息次数累计。休息结束后移除刷新定时器。

## 提醒与退出

通过 `EnumDisplayMonitors` 枚举当前显示器，使用完整 `rcMonitor` 矩形覆盖任务栏，保留负坐标。全部窗口完成首次绘制并确认可见后开始共用同一截止时间的倒计时。各屏幕内容按其 DPI 缩放并居中。兼容启动器传入的隐藏或最小化状态。

窗口不提供标题栏、边框、关闭或最小化按钮，忽略 Esc、关闭消息及关闭、最小化、移动、缩放等系统命令。休息期间控制窗口拒绝托盘命令与关闭请求，避免提前结束。刷新倒计时时同时维护置顶和可见状态，不持续抢夺键盘焦点。显示器布局变化时准备全套新窗口再替换旧窗口，保留原截止时间；DPI 变化时重新贴合显示器范围。重建失败时记录日志并取消本次休息，避免留下残缺窗口。

定时器或窗口初始化失败时取消本次休息。关键状态与错误通过 `OutputDebugStringW` 记录。

倒计时结束统一销毁所有提醒窗口。锁屏、注销、会话断开和挂起仍可取消当前休息；普通关闭、暂停和退出命令必须等待休息结束后操作。程序不封锁输入，也不拦截系统安全界面或任务管理器；置顶机制不能保证覆盖所有其他置顶窗口或独占全屏应用。

`--smoke-test` 使用独立互斥量、默认配置和 2 秒提醒，创建设置与提醒窗口后自动退出，不保存配置或修改启动项。

## 配置、启动和权限

启动时读取 `GetUserDefaultUILanguage()`，主语言为中文时统一显示简体中文，其他语言或未知值使用英文。语言在本次进程内固定，系统语言改变后重启生效；不使用地区格式、键盘布局或 INI 来判断语言。托盘、设置、休息窗口、错误提示均通过集中式文案表获取文本，内部调试日志和代码注释仍使用中文。

设置资源保留语言中性的控件结构，在 `WM_INITDIALOG` 中填充本地化文案并设置标题；静态标签使用独立资源 ID。中英文共用布局，冒烟测试核对实际控件文字及文字尺寸。仅 `--smoke-test-zh` 和 `--smoke-test-en` 在测试进程内覆盖语言，不改变用户系统设置或生产配置；图形测试串行运行，避免测试互斥量冲突。

配置文件不存在时视为首次运行，自动打开设置，默认勾选“登录时自动运行”，只有点击保存才注册启动项。取消或关闭不保存配置、不更改启动项，本次不反复弹窗，下次启动仍提示。已有配置时不自动弹窗，设置勾选状态读取实际启动项；新增可选路径字段，其他历史设置保持兼容。文件访问失败不视为首次运行。若启动时会话或桌面不可用，首次设置延后至会话恢复后显示。

INI 路径为 `%LOCALAPPDATA%\TakeABreak\settings.ini`；仅保存设置时生成目录与文件。使用 UTF-16LE 的 Win32 INI，临时文件写入并刷新后替换正式文件。缺失键回退默认值；非法值回退并通过托盘提示。`Version=1` 标识首版格式，尚未承诺未来版本配置兼容。

提醒间隔为 1～1440 分钟，休息时长为 1～3600 秒。自动启动状态以 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 中的 `TakeABreak` 值为准，不在 INI 中重复存储。路径加引号，以支持空格。

INI 的 `General/ExecutablePath` 保存当前 EXE 绝对路径。主实例每次启动核对已有配置和启动项：存在本工具的非空启动项且指向其他路径时，重写为当前 EXE 的带引号路径；启动项不存在时只更新 INI，不自动启用。旧 INI 缺少路径时补录，首次没有 INI 则等用户保存后再记录。路径按 Windows 常规不区分大小写比较，拒绝相对路径以及包含换行或引号的值。启动项读写失败时不更新已记录路径，以便下次重试；INI 保存失败也保留旧记录。注册表成功而 INI 失败时，下次只需补录 INI。测试通过注入启动项读写替身验证迁移，不触碰真实启动项。

进程清单使用 `asInvoker`，程序不请求管理员权限，也不提供提权重启入口。保留 `Version=1` 配置兼容：旧的 `RestrictInput` 字段被忽略，保存时不再写入，其他设置保持不变。

“移除自动启动并退出”只移除本工具的当前用户启动项并退出，不删除 EXE 或配置，也不涉及系统服务。

## 首版边界

- 支持多显示器全屏提醒及 DPI 缩放，不提供系统级输入封锁或防进程终止机制。
- 不实现空闲检测、使用统计、联网、更新下载、持久化运行日志或系统服务。
- EXE 大小、工作集和 CPU 目标必须实测，不能仅凭技术选型宣称达成。
- 名称暂用 Take a Break，已有同名开源项目，未宣称名称唯一；许可证为 MIT。
