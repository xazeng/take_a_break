# 工程规约

- 默认使用简体中文沟通，项目说明及新增注释使用简体中文。
- 修改前提出范围、兼容性和验证方案，获得确认后执行；构建及间接触发编译的测试须获授权。
- 采用 C++17、原生 Win32 API、MSVC 和 CMake；不引入大型运行时或界面框架。
- 支持 Windows 10（1607 及以上）/Windows 11 x64，首版不承诺其他系统和架构兼容。
- 默认使用静态 C/C++ 运行库，发布主体为单个 `TakeABreak.exe`。
- 不安装 Windows 服务。自动启动仅使用当前用户的登录启动项，由用户在界面中启用。
- 首次运行以配置文件不存在为准，自动打开设置并默认勾选登录自启动；必须保存后才注册，取消不得修改启动项，已有配置保持原行为。
- 空闲时不轮询；使用窗口消息、会话事件及提醒定时器。仅休息期间更新倒计时。
- 仅提供倒计时提醒，不限制键盘鼠标输入，不提供管理员提权入口。旧配置的 `RestrictInput` 字段忽略，保存时不再写入。
- 休息时每个显示器全屏置顶并共用倒计时；窗口及托盘操作不可提前关闭或最小化提醒。保留锁屏、注销、挂起时取消提醒的流程，不阻拦系统安全界面。
- 锁屏、注销、重启等影响当前桌面的验证须单独获得授权；界面冒烟测试不保存用户配置或修改启动项。
- 编译产物和本地测量材料放在已忽略的 `build/`、`artifacts/` 下，不提交。
- 架构及业务行为维护在 `DESIGN.md`；最终需求及落地方案按时间追加到 `AGENTS.log.md`，不在执行日志中记录验证结果。
