# 执行日志

## 2026-09-17

确定开发 Windows 原生轻量休息工具“起来歇会儿 / Take a Break”，首版采用 C++17 + Win32、MSVC 静态运行库和 MIT 许可证，目标平台为 Windows 10/11 x64。采用无系统服务的单 EXE 托盘架构，平时无任务栏按钮；登录启动后按可配置间隔提醒，解锁后重新计时，锁屏及会话不可用时取消休息，保留手动暂停。实现原生设置窗口、整点模式、倒计时提醒、同线程封锁与恢复输入及失败降级、单实例保护、当前用户登录自启动和移除自启动菜单、本地 INI 配置，并补齐构建配置、工程规约、设计及使用文档。

### 首次启动设置

首次启动以配置文件尚不存在为准，自动打开设置窗口并默认勾选“登录时自动运行”；保存后才注册启动项，取消或关闭不保存，下次启动继续提示。已有配置和启动项状态保持兼容；不可用会话下延后首次设置窗口至会话恢复后显示。

### 移除输入限制与管理员重启

移除键盘鼠标输入限制、工作线程及“以管理员身份重启”入口和相关逻辑，保留倒计时提醒、托盘、暂停及登录自启动。设置界面同步精简；旧 INI 的 `RestrictInput` 字段被忽略，保存时不再写入，其他配置保持兼容。同步更新工程规约、设计和使用说明。

### 多显示器全屏提醒

改为每个显示器显示全屏置顶提醒，覆盖任务栏并共享倒计时，适配负坐标、DPI 和显示器布局变化。移除提前结束入口，休息期间忽略关闭、最小化及托盘控制命令；倒计时结束统一释放窗口。保留锁屏、注销和挂起时取消提醒，不恢复输入封锁或提权功能，INI 格式不变。

### A 方案图标

采用用户选定的绿色伸展人物与计时环图标，整理正式 PNG 并生成多尺寸 ICO，嵌入程序资源，统一替换 EXE、托盘和窗口图标，保持单 EXE 发布方式。

### 覆盖原运行路径

用户关闭旧版后，将含 A 方案图标的新版 EXE 覆盖至 `build/Release/TakeABreak.exe`，沿用原运行路径。
