# 首版验证记录

本文前部保留首版的历史测量；当前功能移除后的验证见文末。首版输入限制替身测试已随功能删除，不再属于当前测试集。

日期：2026-09-17。

## 构建环境和结果

- Windows 10，系统版本 10.0.19044，x64。
- Visual Studio 2022，MSVC 19.44.35227，Windows SDK 10.0.26100.0，CMake 3.31.2。
- x64 Release 构建通过，无编译或链接警告。
- 产物：`build/Release/TakeABreak.exe`，192,000 字节，即 187.5 KiB。
- `dumpbin /dependents` 仅显示 Windows 系统 DLL：USER32、GDI32、SHELL32、ADVAPI32、ole32、WTSAPI32、KERNEL32；无需附带 VC 运行库 DLL。

## 自动验证

`ctest --test-dir build -C Release --output-on-failure`：2/2 组通过。

核心测试覆盖：整点边界、间隔模式、倒计时取整与过期、暂停状态、缺失和损坏 INI、有效配置往返、时长范围校验。输入接口替身覆盖同一工作线程封锁/恢复、UI 不处理消息时仍自动恢复、主动取消、封锁失败后不错误解除其他线程的封锁。未调用真实 `BlockInput`。

界面冒烟测试覆盖：后台控制窗口隐藏且没有任务栏样式、设置窗口可见且字段初始化正确、提醒窗口可见并置顶、倒计时自动销毁窗口、重新计时后忽略旧定时器消息、不可用会话不受旧消息触发、解锁不取消手动暂停，以及退出路径。

另以隐藏启动方式运行安全预览，进程自动退出并返回 0。图形自动化工具未能枚举本程序的托盘工具窗口，未取得截图进行目视排版验收；不能将上述程序内断言视作截图验收。

正常模式单实例检查：第二实例及时退出，退出码 0。资源测试前确认不存在用户配置，测试后仍未生成配置文件；未启用自动启动。

## 资源测量

最终 Release 程序启动后等待 3 秒，再在正常托盘空闲状态采样约 30 秒；使用进程工作集、私有字节及累计 CPU 时间，不主动裁剪工作集。

| 指标 | 实测值 | 对照目标 |
| --- | --- | --- |
| EXE | 192,000 字节（187.5 KiB） | 小于 1 MB，达标 |
| 空闲工作集 | 11,857,920 字节（约 11.31 MiB） | 低于 10 MB，未达标 |
| 私有字节 | 2,236,416 字节（约 2.13 MiB） | 补充口径，不代替工作集 |
| 30.015 秒内新增 CPU 时间 | 0 毫秒 | 本次样本接近 0%；受系统计时分辨率限制 |

这是当前机器的一次测量，不代表所有机器或长期运行结果。资源原始记录位于本地忽略目录 `build/idle-metrics.json`。

## 待交互验收

以下未实测，不能宣称通过：

- 实际锁屏/解锁、注销/重新登录、切换用户、睡眠/恢复以及远程会话断开/重连。
- 实际启用/移除登录启动项。
- Explorer 重启后的托盘恢复。
- Windows 11、多个 DPI、主显示器变更及不同桌面主题的目视布局。

执行影响当前桌面的锁屏或注销测试前，需要用户单独授权。

## 首次启动设置调整验证（2026-09-17）

调整后 x64 Release 构建无警告，CTest 2/2 组通过。新增检查覆盖配置缺失与保存后的首次运行判定、首次设置默认勾选自启动、取消后本次不重复弹窗且启动项状态不变、已有配置时不自动弹窗及手动打开设置时按实际启动项初始化。测试不保存用户配置、不修改真实启动项。上文资源数据为首版测量，本次未重复测量资源占用。

## 移除输入限制与管理员重启（2026-09-17）

x64 Release 构建无警告，CTest 2/2 组通过。删除输入线程及其替身测试；新增旧 INI 中 `RestrictInput=1` 被忽略、其他设置保留、保存后旧字段消失的兼容检查。现有首次启动、取消、托盘后台窗口、置顶提醒、自动倒计时结束和暂停策略的冒烟测试通过。

通过 `dumpbin /imports` 确认发布 EXE 不再导入 `BlockInput` 或 `ShellExecuteExW`。当前 EXE 为 188,928 字节（184.5 KiB）；本次未重测空闲资源占用，也未修改真实登录启动项。设置窗口删除相关控件并缩短高度，尚未进行截图目视验收。

## 多显示器全屏提醒（2026-09-17）

x64 Release 构建无警告，CTest 2/2 组通过。当前桌面为两块 1920×1080 显示器，坐标分别为 `(0, 0)` 和 `(-1920, 0)`。冒烟测试逐屏核对窗口数量、完整显示器矩形、置顶属性，以及无标题栏、系统菜单和最小化按钮。

测试直接发送 Esc、关闭和最小化系统命令，确认窗口保持可见；向控制窗口发送暂停、退出、移除启动项和关闭命令，确认休息不中断。模拟显示器变化后确认窗口数量和倒计时截止时间不变，倒计时结束后窗口统一释放；模拟会话不可用后确认全部提醒窗口销毁。未改变真实锁屏状态或登录启动项。

尚未进行物理显示器热插拔、混合 DPI、Windows 11 或截图目视验收。多屏全屏测试不代表能覆盖系统安全桌面、独占全屏应用或所有其他置顶窗口。本次未重新测量资源占用。

## A 方案图标（2026-09-17）

确认 ICO 含 16/20/24/32/40/48/64/128/256 九种尺寸。原 Release EXE 被运行实例占用，未终止该实例；改在 `build/icon-release` 独立构建，新 EXE 位于 `build/icon-release/Release/TakeABreak.exe`。构建无警告，新目录 CTest 2/2 组通过，启动时成功从资源加载大小图标。EXE 为 292,352 字节，仍小于 1 MB。未将针对旧构建目录的测试结果计入新版验证。

## 自动选择中英文（2026-09-17）

在 `build/icon-release` 构建 x64 Release，无警告。CTest 4/4 组通过：核心逻辑、跟随系统语言冒烟、简体中文冒烟和英文冒烟。覆盖简体/繁体/香港中文映射、英文与未知语言回退、中文及英文配置错误信息、设置实际控件文字、当前字体下标签尺寸、提醒窗口标题及说明。原有双屏倒计时和关闭限制检查继续通过。

语言测试只切换进程内语言，没有修改 Windows 显示语言、用户 INI 或真实启动项。未在独立英文 Windows 安装或其他 DPI 环境做目视验收；本次未重测性能。

新版已覆盖原路径 `build/Release/TakeABreak.exe`，与测试产物 SHA-256 一致，文件大小为 297,472 字节。

## 路径迁移与单实例唤起（2026-09-17）

在 `build/icon-release` 构建 x64 Release，无警告；CTest 4/4 组通过。核心测试覆盖旧 INI 补录绝对路径、已有启动项迁移、关闭自启动时不启用、读写失败保留旧路径并可重试、首次未保存时不生成配置以及拒绝相对路径。启动项通过替身验证，未修改真实注册表启动项。

三组界面冒烟测试均启动真实第二进程，验证其通知主实例打开设置后退出；休息期间的设置请求延迟到倒计时结束后显示。测试使用独立互斥体和控制窗口，不通知正常运行实例。未进行真实锁屏、注销或登录验证，本次未重测性能。

新版已覆盖 `build/Release/TakeABreak.exe`，与测试产物 SHA-256 一致，文件大小为 306,176 字节。

## 设置运行状态与 README（2026-09-18）

x64 Release 构建无警告，CTest 4/4 组通过。三组界面冒烟验证新增说明的中英文控件文字与尺寸，以及暂停、等待解锁、未启动计时、到期归零、24 小时边界和剩余秒数变化的实际控件显示；沿用单实例和全屏休息检查。未修改用户 INI 或真实启动项，未进行真实锁屏和截图目视验收，也未重测资源占用。

新版覆盖 `build/Release/TakeABreak.exe`，SHA-256 与测试产物一致，文件大小为 309,248 字节。
